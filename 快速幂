#include <bits/stdc++.h>
using namespace std;
long long cheng(long long a,int b){
	long long ans=1;
	while(b){
		if(b%2) ans=ans*a;
		a=a*a;
		b=b/2;
	}
	return ans;
}
int main(){
	int a,n;
	cin>>a>>n;
	/*
	int ans=1;
	while(n){
		if(n%2) ans=ans*a;
		a=a*a;
		n=n/2;
	}
	cout<<ans;
	*/
	cout<<cheng(a,n);
	return 0;
}
