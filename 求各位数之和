#include <iostream>
#include<string>
using namespace std;


int main(){
	string n; cin>>n;     //定义一个字符串存数字
	int len=n.size(),sum=0;  //获取字符串长度
	for(int i=0;i<len;i++){
		int num=n[i]-'0';   //通过 ASCII码相减得到数字并求和
    sum+=num;
	}
	cout<<sum;
	return 0;
}
