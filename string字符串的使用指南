#include<iostream>
#include<string>
using namespace std;
int main(){
    //定义字符串
    string str;    //最常用，定义空字符串
    const char *str="hello"; 
    string s1(str);    //使用字符串str初始化
    string s2(str);    //用一个对象初始化另一个对象
    string(数字,'字母');  //用n个字母初始化
    
    //赋值
    str1="hello";   //直接用"="号赋值,支持单个字符，用''
    str2=str1;       
    str4.assign("hello");      //都用了assign函数进行赋值操作，这里是拷贝一个字符串
    str5.assign("helloworld",5)     //这里只采用了字符串的前5个字母，即"hello"
    str6.assign(str5);       //同str6=str5
    str7.assign(10,'s')      //多个字符赋值

    //字符串拼接
    str+="hello";  str+=':';      //通过“=”号拼接
    string str2="genshin";
    str1+=str2;     //将一个字符串拼接到另一个字符串后面
    str3.append("love");   //利用append拼接其他字符串
    str.append("genshin impact",4);   //只取前四个字符
    str3.append(str2);
    str.append("genshin",x,y);      //表示从x位开始，添加y个字符

    //查找和替换
    str.find("fe");  str.rfind("fe");   //用于查找字符或串，find是从左到右，rfind是从右到左，返回的值一致
    str.replace(1,3,"1111")      //从1号位起。3个字符，替换为“1111”(注意字符串从0开始)，如"abcdefg"替换为"a1111efg"

    //比较
    s1.compare(s2);    //s1与s2通过ASCII码比较，相等则返回0，不等则返回1/-1

    //插入和删除
    str.insert(1,"111");    //在第一位插入这个字符串
    str.erase(x,y);    //在x位置删除y个字符

    //子串
    string str="abcdefg";
    string ss=str.substr(1,3);   //通过substr提取子串
