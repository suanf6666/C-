#include<iostream>
#include<string>
using namespace std;
int main(){
    //定义字符串
    string str;                  //最常用，定义空字符串
    const char *str="hello"; 
    string s1(str);              //使用字符串str初始化
    string s2(str);              //用一个对象初始化另一个对象
    string(数字,'字母');          //用n个字母初始化
    
    //赋值
    str1="hello";                //直接用"="号赋值,支持单个字符，用''
    str2=str1;       
    s.assign("hello");           //都用了assign函数进行赋值操作，这里是拷贝一个字符串
    s.assign("helloworld",5)     //这里只采用了字符串的前5个字母，即"hello"
    s.assign(str5);              //同str6=str5
    s.assign(10,'s')             //多个字符赋值
    getline(cin,s);              //输入到s里,可以有空格

    //字符串拼接
    s+="hello";  str+=':';          //通过“=”号拼接
    string str2="genshin";
    str1+=str2;                     //将一个字符串拼接到另一个字符串后面
    st.append("love");              //利用append拼接其他字符串
    s.append("genshin impact",4);   //只取前四个字符
    s.append(str2);
    s.append(str,pos,len);          //表示在s末尾,添加str里str[pos]处开始的len个字符
          例:s="accbs",f="yttrs";
             s.append(f,1,3);       //则s=accbsttr

    //单字符操作
    s.push_back('x');           //在末尾插入字符x或其他字符
    s.pop_back();               //删除尾部字符,C++11及以上适用

    //查找和替换
    s.find();  s.rfind();       //用于查找字符或串，find是从左到右，rfind是从右到左,用法一样但匹配位置可能不一样.
    s.find(str,pos);            //从s[pos]位置开始找,不填则默认从0开始找.返回出现str子串里第一个字符的下标,找不到返回string::npos,是1或一个大数字
    s.find("str",pos,len);      //只适合C风格字符串,不支持string.起找位置一样,但只找str里前len个字符,符合则返回下标
    s.replace(pos,len,str);      //从s[pos]处将len个字符,替换为str字符串内容，如str="11111"."abcdefg"替换为"a11111efg"
    if(s.find()!=string::npos)  //找不到

    //比较与翻转
    s1.compare(s2);         //s1与s2通过ASCII码比较，相等则返回0，不等则返回1/-1
    reverse(s.begin(),s.end());   //翻转字符串s

    //插入和删除
    s.insert(pos,s2);       //在s[pos]处插入字符串s2,pos是必须的
    s.insert(pos,len,'x')   //在s[pos]处插入n个字符'x',只能是字符
    s.erase(pos,len);       //在s[pos]处删除len个字符,使用方式和substr类似

    //子串
    s.substr(pos,len);      //在s[pos]处截取长度为len的子串,不填len默认截完,不填pos默认从0开始截取
          例:string str="abcdefg";
          string ss=str.substr(1)/str.substr(1,3);

    //类型转换
    stoi(s,&pos,10);               //将s中数字转换为10进制的int类数字,填2就按2进制处理成10进制,其他类似,不填就按10进制直接转换
          例:string s="11x22";     //&pos返回第一个无法解析的符号下标,是一个数字
          size_t pos=0;            //也有stol,stoll,stod等类似函数,相应转换成long int,long long等类型.
          int r=stoi(s,&pos);      //则r=11,pos=2.
                                   //若int r=stoi(s,&pos,2); 则r=3
     string s=to_string("123");    //利用to_string将其他类型数据转换为字符串
}
